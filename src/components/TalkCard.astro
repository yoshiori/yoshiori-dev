---
interface Props {
  title: string;
  url: string;
  thumbnail?: string;
  date: string;
}

const { title, url, thumbnail, date } = Astro.props;

const formattedDate = new Date(date).getFullYear
  ? new Date(date).toLocaleDateString('en-US', { year: 'numeric', month: 'short' })
  : date;
---

<a class="block no-underline border border-border bg-surface overflow-hidden transition-all duration-200 hover:border-[#444] hover:-translate-y-0.5" href={url} target="_blank" rel="noopener">
  {thumbnail
    ? <img class="w-full aspect-video object-cover block bg-[#1a1a1a]" src={thumbnail} alt={title} loading="lazy" />
    : <div class="w-full aspect-video bg-[#1a1a1a] flex items-center justify-center">
        <span class="font-mono text-[10px] tracking-widest text-border">SPEAKERDECK</span>
      </div>
  }
  <div class="p-3.5">
    <div class="text-xs text-text leading-relaxed mb-2">{title}</div>
    <div class="font-mono text-[10px] tracking-wider text-muted">{formattedDate}</div>
  </div>
</a>
